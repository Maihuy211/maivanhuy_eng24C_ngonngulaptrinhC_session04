#include <stdio.h>

int main (){
	int number;
	printf("moi ban nhap mot so: ");
	scanf("%d",&number);
	if(number%2==0){
		printf("la so chan");
		}
	else{
		printf("la so le");
	}
	return 0;
	}
